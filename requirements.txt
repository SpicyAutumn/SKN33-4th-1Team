# 수업 권장 Python: 3.12
# 프로젝트 착수 후 팀 공통 환경에서 검증한 직접 의존성 버전을 고정합니다.

# Environment and data
python-dotenv
pandas
pypdf
requests

# LLM / RAG
# 13_lm_evaluation/04_ragas.ipynb의 버전 조합을 따릅니다.
# openai는 instructor의 jiter 범위와 충돌하지 않는 2.45.0으로 고정합니다.
# ragas 0.4.3은 langchain-community 0.4.x에서 삭제된 VertexAI 모듈을 여전히
# import하므로, ragas_evaluator의 호환 셔임이 그 자리를 채웁니다.
openai==2.45.0
langchain==1.3.17
langchain-openai==1.6.0
langchain-community==0.4.2
langgraph
ragas==0.4.3

# Vector DB: 현재 수업 예제 기준의 임시 선택, 주제 확정 후 교체 가능
pinecone
langchain-pinecone

# Application and test
# st.iframe과 st.html을 사용합니다. 아래는 동작을 확인한 버전입니다.
streamlit>=1.62
pytest
