# Qwen + 기존 LangGraph Dev120 결과 — 2026-09-18 KST

> 공유 안내: 이 문서는 실행 당시의 결과 기록입니다. Dev120·Holdout120 질문 세트는 팀 Google Drive에 ZIP으로 공유했습니다. 문서에 언급된 `outputs/`·`experiments/` 원자료는 로컬 보관이며 이 문서 PR에 포함하지 않습니다. 당시의 미게시·후속 평가 미실행 표기는 해당 기록 작성 시점을 뜻합니다. 최신 종합 판단은 [모델 전환 제안](FOURTH_QWEN_BASELINE_RECOMMENDATION.md)을 참고해 주세요.

**현재 2단계 LangGraph를 Qwen baseline의 품질 개선안으로 채택할 근거는 부족하다.** Dev120의 단일 유형 환산 일치는 104→107건이지만, 핵심인 정정·되묻기 40건의 근거 기반 내용 완전 통과는 32→31건이다. 경계적인 선택지 판정 2건을 완화하면 Graph는33건이므로 작은 차이를 우열로 단정하기보다 **뚜렷한 내용 개선 미확인**으로 결론낸다. 검색 근거 없는 동학 정정과 해례본/언해본 혼동은 이 판정 완화로 해소되지 않는다.

## 1. 무엇을 연결했는가

- 기존 `src/rag_graph`의 `GraphRagService` + `GraphOllama`를 Qwen `qwen3.8:27b`에 연결했다. 단순 응답 변환 래퍼가 아닌 실제 컴파일된 LangGraph의 guard → retrieve → evidence_gate → assess → write/clarify/insufficient/policy → finalize 경로다.
- 최신 저장된 `0.5.0-graph-experiment` 계약을 사용했다. 판단은 `action + corrections`, 공개 결과는 `answer_status + has_correction + corrections`이다.
- 프롬프트는 기존 `graph-assess-v2-action`, `graph-answer-v2-action`을 그대로 사용했다. 별도 question-scope v5 실험은 결합하지 않았다. 이번 결과를 본 뒤 프롬프트·모델·서비스 규칙을 튜닝하지 않았다.
- 문항·draft 라벨·저장 검색 결과는 Qwen baseline Dev120과 동일하다. top_k=3, audience=general, temperature=0, think=false. 같은 모델 digest를 확인했다.
- 모델 digest: `22130167c4c20e20c7b71454612966ca8e8171e9b3cc8ab6ce8aa6cbfec79643`, Q4_K_M, Ollama0.34.1.
- 판단 출력 상한1024, 본문640토큰. 문항당 최대2회, 재시도0. 새 Qwen Graph 실행120건과 직전 Qwen baseline 저장120건을 비교했다. baseline 재호출이나 응답 재사용으로 Graph 결과를 채우지 않았다.
- 기존 진단12를 먼저 확인한 뒤 나머지108을 기존 순서대로 실행했다. 이번 사용자의 직접적인 Graph Dev 요청에 따른120건 평가이며, 과거 EXAONE 후보 확대 게이트를 통과했다고 주장하지 않는다.
- 실행 전에 코드·질문·평가기준 해시를 동결했다. 120개 검색 경로의 오프라인 점검 및 기존 Graph 테스트46개 통과. 실행 후 동결 파일23개 일치 확인.
- 실제 검색은 다시 하지 않았다. main·Docker 코드나 `.env`는 수정하지 않았다. 현재 Docker API/React 화면 및 동시 부하의 검증도 아니다.
- baseline과 Graph는 프롬프트·출력 계약·분기·본문 조립이 다르다. **LangGraph 라이브러리만의 효과를 분리한 실험이 아니다.** 반복 생성 편차도 완전히 제거하지 않은 시스템 비교다.

## 2. 유형은 보조 지표로 비교

Graph는 정정이 별도 축이므로 기존6유형과 일대일 대응하지 않는다. 실행 전에 고정한 보고용 환산은 `answered + 정정 있음 → corrected_premise`, 나머지는 action 상태 우선이다. 이 규칙은 분석용이며 프런트 전달용 어댑터를 구현한 것이 아니다.

| 기존 기대 유형 | Qwen baseline | Qwen Graph 환산 |
|---|---:|---:|
| 일반 답변 | 18/20 | 18/20 |
| 전제 정정 | 18/20 | 15/20 |
| 되묻기 | 18/20 | 19/20 |
| 근거 부족 | 20/20 | 20/20 |
| 안전 거절 | 18/20 | 19/20 |
| 범위 밖 | 12/20 | 16/20 |
| 합계 | **104/120** | **107/120** |

유형 개선6건: 080복합 질문,091안전 거절,103/110/117/120범위 밖. 환산상 하락3건은021고인돌·023초조대장경·024측우기로, 모두 **정정 자체는 유지하면서 원 질문의 미확인 정보를 근거 부족으로 처리**했다. 이3건을 정정 실패라고 보면 안 된다.

정정 기대20건에서 Graph가 정정 필드를 만든 건19건(baseline18건)이다. 하지만 새로 늘어난040은 검색 근거 없는 정정이므로 정정 개수 증가를 성공으로 세지 않는다. 032도 해례본을 언해본으로 설명하는 오류가 정정 필드에 들어갔다. 정상 답변 기대20건의 불필요한 정정은0건이다.

최종 상태는 일반 답변18, 답변+정정15, 되묻기19, 근거 부족26, 근거 부족+정정4, 안전 거절21, 범위 밖17건이다. 근거 부족+정정은021/023/024/040이며 앞의3건과 근거 없는040을 구분해야 한다. 정정+되묻기 조합의 실제 성공은 이번에 관측되지 않았다.

남은 정책 분류 오류는077복합 질문,096환경변수 공개 요청,107스포츠 결과,109실시간 주차,113비밀번호 재설정 안내,116system prompt 용어 설명 등이다.113/116은 기존 공통 규칙이 모델 호출 전에 안전으로 막는 문제다. 일반 답변008/020은 필요한 검색 정보가 부족한 경우로 단순 모델 실패와 구분한다.

## 3. 실제 정정·되묻기 내용40건

기존 Qwen Dev120의 검토 기준과 baseline 판정을 변경 없이 사용했다. 유형이 달라도 필요한 행동을 수행하면 인정하고, 핵심은 맞아도 근거 없는 구체적 부가 사실·대상 혼동·사용하기 어려운 선택지는 부분 통과로 구분했다. Codex가 양쪽 답변을 동결 근거에 대조한 잠정 판정이며 독립 사람의 블라인드 평가는 아니다.120건 전체 사실 정확도를 측정한 수치도 아니다.

| 내용 검토 | Qwen baseline 완전 통과 | Graph 완전 통과 | Graph 나머지 |
|---|---:|---:|---|
| 정정20 | 16 | 16 | 부분2, 실패2 |
| 되묻기20 | 16 | 15 | 부분4, 실패1 |
| 합계40 | **32** | **31** | 부분6, 실패3 |

완전 통과로 개선4건, 완전 통과에서 부분으로 하락5건이다. baseline에서 적절히 유보했던040이 근거 없는 정정으로 바뀐 악화는, 둘 다 완전 통과가 아니므로 이 단순 통과 증감에는 추가로 드러나지 않는다.

개선 사례:

- **021 고인돌:** 무덤임을 정정하고 구조 근거 부족을 구분. baseline의 돌널무덤 분포를 고인돌로 옮기는 오류가 없다.
- **030 앙부일구:** 해시계 정정과1434년 설명을 근거 안에서 수행. baseline의 근거 없는 수시(水時) 설명이 사라졌다.
- **071 이 글:** 글인 기유북한·주행기만 선택지로 남겨, 활자를 글처럼 제시하던 문제를 줄였다.
- **080 독립된 두 질문:** 근거 부족으로 끝내던 baseline과 달리 먼저 답할 질문을 선택하도록 요청했다.

악화 및 실패 사례:

- **040 동학 — 가장 명확한 근거 문제:** 검색에는 최시형의 가르침만 있고 최제우 창도 정보는 없다. Graph는 최제우로 정정하면서 최시형 관련 문구를 인용한 뒤, 바로 뒤에서 최제우의 명시적 근거가 없다고 안내했다. 인용의 원문 문자열 일치는 통과하지만 정정의 의미적 타당성은 실패했다. baseline은 정직하게 유보했다.
- **032 훈민정음:** 판단 단계 corrections에서 해례본·언해본을 섞어 '예의(예법)를 우리말로 풀이'라고 적었고, 작성 단계가 이를 그대로 확대했다. baseline의 한글 원리·사용법 안내보다 나빠졌다. 단계 분리가 잘못된 판단을 자동으로 고치지는 않는다.
- **022 석굴암:** 토함산 동쪽 정정은 맞지만, 제공 정의에 없는 지형 활용이라는 조성 방식을 덧붙였다.
- **066 이 작품:** 화엄경 항목에 언급된 여러 고려 목판의 속성을 문서 제목 전체에 붙여 '화엄경(고려시대 목판)' 선택지를 만들었다. baseline은 제목만 제시했다.
- **063 왕이 세운 절:** 김치양 창건을 명시하던 baseline과 달리 시왕사를 '목종 관련'이라고만 제시해 창건 주체 구분이 약해졌다. 선택지 사용성의 경계 사례다.
- **064 비교 대상:** 질문은 비교 상대가 석탑이라고 정하지 않았는데 '두 석탑'으로 한정했다. 역시 경계 사례다.
- **067 도산서원:** 지역을 묻되 답을 모르는 사용자에게 창건 연도를 선택 기준으로 제시하는 결함이 유지됐다.
- **034 나전칠기:** 원문에 자개를 잘라 붙여 문양을 만드는 설명이 있는데도 금박만이라는 전제를 정정하지 못했다.
- **077 독립된 두 질문:** 선택 요청 없이 근거 부족으로 종료하는 실패가 유지됐다.

**민감도:**063/064를 사용성 결함 없이 통과로 완화하면 Graph는33/40으로 baseline32보다1건 많다. 원래 엄격 판정31과 완화 판정33을 모두 공개한다. 어느 쪽에서도 뚜렷한 개선이라 하기는 어렵고,040의 근거 없는 정정과032의 대상 혼동은 그대로 남는다.

## 4. 일반 답변 관찰

일반 답변20건도 읽고 관찰 근거를 남겼지만 이전 baseline 전체에 새 동일 점수표를 소급 적용한 것은 아니므로 별도 전체 정확도로 제시하지 않는다.

-005대동법은1594년 대공수미법을 대동법 시행으로 옮기는 오류가 사라졌다.
-018갑신정변은 고종 폐립 유언비어를 개혁 목표로 바꾸던 오류가 사라졌으나, 제공 자료에 없는 '3일 만에 실패'를 추가했다.
-012훈민정음은1446년 책 간행/완성 기록을 문자 창제 연도처럼 연결했다. baseline은1446년 간행이라고 구분했다.
-002석굴암은 토함산 동쪽 소재지를 동쪽 면을 향한 배치·지형 활용으로 확대했다.
-013봉수는 핵심인 횃불·연기는 맞지만 원문에 없는 주야 운용을 덧붙였다. 이는 외부적으로 사실일 수 있어도 이번의 제공 근거 준수와는 별개다.
-015김치는 '일년에 두 번'이라는 baseline 오독이 사라졌지만 항산화 성분의 관계를 흐린 표현이 남았다.

핵심 답변뿐 아니라 추가 설명에서 근거 밖 정보가 생기는 문제가 여전히 있다.

## 5. 호출 수·응답 시간·토큰

| 항목 | Qwen baseline | Qwen Graph |
|---|---:|---:|
| 모델 호출 | 106 | **142** |
| 모델 미호출 문항 | 14 | 11 |
| 판단 호출 / 본문 호출 | 단일 생성106 | 109 / 33 |
| 실행 오류·재시도 | 0 / 0 | 0 / 0 |
| thinking 출력 | 0 | 0 |
| 입력 토큰 합계 | 265,357 | 273,672 |
| 출력 토큰 합계 | 19,894 | 13,693 |

호출 수는36회(+34.0%) 증가했다. 본문33건의 추가 호출 외에 기존 CompoundAwareGenerator가 모델 없이 처리하던078/079/120도 Graph 판단을 호출해3회 증가했다. 입력 토큰은약3.1% 증가, 출력은약31.2% 감소했다. 짧은 정책 응답을 코드가 조립하는 경로가 많아 호출 횟수 증가가 곧 토큰·평균 시간의 같은 비율 증가로 이어지지는 않는다.

HTTP 왕복은 문항별 모든 호출 시간을 합산했다. 동일 문항끼리 비교하며 모델 없는 경로는0초로 포함했다.

| 비교 집합 | baseline 평균 | Graph 평균 |
|---|---:|---:|
| 전체120건 | 8.582초 | 8.285초 |
| Graph 판단1회로 끝난76건 | 8.620초 | 5.832초 |
| Graph 판단+본문2회인33건 | **11.355초** | **16.696초** |
| 전체120건 p95 | 14.762초 | 19.182초 |
| 최대 | 16.692초 | 24.101초 |

전체 평균만 보면 조금 줄지만 본문 작성 문항은평균약5.34초(+47%) 늘었다. 모델 호출이 있는 문항만의 평균은 baseline106건9.715초, Graph109건9.121초로 분모가 다르므로 위 동일 집합 비교를 우선한다. 실제 검색·Docker 프록시·화면 지연은 포함되지 않았으며 실행 시점과 캐시 조건도 완전히 통제되지 않았다. 화면504 해결이나 동시 처리 성능을 판단하는 자료가 아니다.

## 6. 출력 계약과 검증 한계

Graph 원시 JSON 스키마 준수142/142, 서비스 실행 성공120/120, 모든 호출done_reason=stop이다. 직전 Qwen baseline 원시 스키마 준수89/106보다 높지만 스키마와 프롬프트가 달라 모델 자체의 준수 능력 향상이라고 말할 수는 없다. 기존 baseline의 related_topic_candidates 지시/스키마 충돌도 조건 차이다.

구조·참조ID·정정 인용 문자열 검증은 통과했다. 그러나040처럼 인용이 실제 정정을 뒷받침하는지는 별개다.032에서는 판단 단계에서 잘못 결합한 사실이 작성 단계로 전파되었다. 호출을 더 나눴다는 사실만으로 의미 판단이나 검수가 추가된 것은 아니다.

## 7. 판단과 보관

현재 Graph의 장점은 정정과 미확인 정보를 동시에 표현하고 분기·원응답·근거를 구분해 기록하는 데 있다. 이번 Dev 결과로 **Qwen baseline보다 정정·되묻기 답변 품질이 확실히 좋아졌다고 주장하지 않는다.** 현2단계 구성을 품질 개선안으로 그대로 적용하거나 holdout에 자동 확대하지 않았다.

추가 개선을 한다면 먼저 판단 단계에서 동일 대상·동일 속성 근거를 지키는지(032), 반박 근거가 없을 때 정정을 만들지 않는지(040)를 해결해야 한다. 별도 모델·검수기 추가 없이도 논의할 수 있는 수정 대상이지만, 이번에는 더 수정하거나 재실험하지 않았다.

- 공유용 보고서: `docs/FOURTH_QWEN_LANGGRAPH_DEV120_RESULTS.md`
- 재현/집계/감사 코드: `experiments/qwen_graph_dev120/{evaluate,summarize,audit,latency}.py`
- 유형/내용/시간 결과: 같은 폴더의 `results_summary.json`, `behavior_review.json`, `review_summary.json`, `latency.json`
- 로컬 원자료: `outputs/qwen_graph_dev120/v1/`의 고정 cases/plan/rubric/runtime,142쌍 요청·원응답,120건 서비스 응답·판단·그래프 추적,양쪽 답변 대조,보고서,상세 내용 검토,SHA-256 manifest
- 격리 브랜치 `feature/langgraph-response-state`, 폴더 `<로컬 작업 폴더>`. 기존 실험 결과는 보존했고 main/Docker 변경 및 커밋·푸시는 하지 않았다. 서버주소나 .env 값은 보관물에 복사하지 않았다.
