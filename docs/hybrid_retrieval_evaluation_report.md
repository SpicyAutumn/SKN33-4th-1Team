# AKS Dense·BM25·Hybrid 검색 평가 보고서

## 결론

현재 RRF 하이브리드 설정은 **정답 문서를 상위 3개 안에 포함하는 비율(Hit@3)**을 Dense 단독보다 1건 높였다(22/25 → 23/25). **1위 정확도(Hit@1)는 19/25로 Dense와 같고**, MRR은 0.800에서 0.820으로 높아졌다. 현재 Dev 25개에서는 Hybrid가 RAG에 여러 근거 후보를 제공하는 기준으로 더 좋은 결과를 보였지만, 고유명사·일반 질문 모두에서 유지되는지는 Holdout과 회귀 세트로 계속 확인한다.

## 평가 조건

| 항목 | 값 |
| --- | --- |
| 실행 시각 | 2026-09-01 15:41:39 (+09:00) |
| 질문 세트 | `data/evaluation/aks_rag_dev_v1.jsonl`의 검색 평가 대상 25건 |
| 구성 | `answered` 20건, `corrected_premise` 5건 |
| 최종 반환 수 | `top_k=3` |
| 후보 수 | `candidate_k=10` |
| RRF 설정 | `rrf_k=60`, Dense 가중치 1.5, BM25 가중치 1.0 |
| 문서당 청크 제한 | 최종 결과에서 같은 `document_id` 최대 2개 |
| Pinecone | `aks-rag-v1` / `__default__` |
| 임베딩 모델 | `text-embedding-3-small` |
| BM25 코퍼스 | 179,028 청크, SHA-256 `444d9fc8e8da5c845610d0a960a43ef04152dd525087e659c511b72382b317e0` |

세 방식은 같은 질문·정답 문서 ID·`top_k=3` 기준으로 비교했다. Hybrid는 Dense 후보 10개와 BM25 후보 10개를 RRF로 합친 뒤, 같은 문서의 청크를 최대 2개로 제한하여 상위 3개를 반환한다. Pinecone은 읽기 전용으로 검색했으며, 벡터를 재적재하거나 변경하지 않았다.

## 결과

| 검색 방식 | Hit@1 | Hit@3 | MRR | 평균 검색 시간 |
| --- | ---: | ---: | ---: | ---: |
| Dense 단독 | 0.76 (19/25) | 0.88 (22/25) | 0.800 | 0.601초 |
| BM25 단독 | 0.20 (5/25) | 0.32 (8/25) | 0.260 | 0.442초 |
| RRF 하이브리드 | **0.76 (19/25)** | **0.92 (23/25)** | **0.820** | 1.043초 |

- **Hit@1**: 기대 문서가 1위에 있는 비율이다. 경복궁처럼 특정 원문을 첫 결과로 보여 주는 사용 경험과 가장 가깝다.
- **Hit@3**: 기대 문서가 상위 3개 안에 있는 비율이다. RAG가 여러 근거 청크를 받아 답변을 만들 때의 문서 발견 성능을 나타낸다.
- **MRR**: 정답 문서가 높게 배치될수록 커지는 순위 지표다. 1위는 1점, 2위는 0.5점, 3위는 약 0.333점으로 반영한다.
- Hybrid의 전체 시간은 Dense 검색 시간과 BM25 검색·RRF 결합·문서당 청크 제한 추가 시간을 합한 값이다. 이번 실행에서 추가 처리 시간은 평균 0.442초였다.

## 관찰한 변화

- `AKS-RAG-DEV-034`는 Dense 단독에서 3위였지만, BM25와 Hybrid에서는 1위가 되어 Hybrid의 MRR 향상에 기여했다.
- `AKS-RAG-DEV-035`는 Dense 단독에서 상위 3위 밖이었지만, BM25에서는 2위·Hybrid에서는 3위가 되어 Hybrid의 Hit@3 향상에 기여했다.
- `AKS-RAG-DEV-013`은 Dense 단독 1위가 Hybrid에서 2위가 되었다. Hybrid가 모든 질문의 순위를 올리지는 않는 사례다.
- `AKS-RAG-DEV-006`, `AKS-RAG-DEV-015`는 세 방식 모두 상위 3위 안에 기대 문서를 찾지 못했다. 질문 표현·청크 구성·후보 수를 추가로 살펴볼 우선 사례다.

## 해석과 다음 단계

BM25 단독은 정확한 단어가 있는 고유명사 검색의 보조 신호로는 쓸 수 있지만, 이 Dev 세트 전체에서는 Dense 단독보다 크게 낮았다. 따라서 BM25만으로 교체하면 안 된다.

현재 Hybrid는 Hit@3와 MRR이 가장 높고 Hit@1은 Dense와 같으므로, RAG가 상위 3개 근거를 받아 EvidenceChecker가 다시 검토하는 흐름에는 적용 후보가 된다. 현재 평가만으로 확정하지 않고 다음 실험을 진행한다.

1. Dense/BM25 가중치와 `candidate_k`를 Dev가 아닌 Holdout에서도 비교한다.
2. 문서당 최대 2개 제한을 적용한 이번 실행에서도 기존 하이브리드 Hit@1·Hit@3·MRR 수치가 유지됐다. 이 제한은 동일 원문의 3개 청크 독점을 막는 다양성 규칙이며, Hit 지표만으로 효과를 완전히 판단할 수 없으므로 답변 근거 품질도 별도로 확인한다.
3. `경복궁`, `향원정`, `석굴암`, `종묘`처럼 고유명사가 뚜렷한 질문은 회귀 테스트로 유지한다.
4. RRF `relevance` 점수는 cosine 점수와 범위가 다르므로 Dense 기준선 `0.40`을 적용하지 않는다.
